/** Enumerator for different scoring options. */
public enum ScoreOption {
  /** No score option. */
  NONE,
  /** Aces score option. */
  ACES,
  /** Twos score option. */
  TWOS,
  /** Threes score option. */
  THREES,
  /** Fours score option. */
  FOURS,
  /** Fives score option. */
  FIVES,
  /** Sixes score option. */
  SIXES,
  /** Small straight score option. */
  SMALL_STRAIGHT,
  /** Large straight score option. */
  LARGE_STRAIGHT,
  /** Full house score option. */
  FULL_HOUSE,
  /** Three of a kind score option. */
  THREE_OF_A_KIND,
  /** Four of a kind score option. */
  FOUR_OF_A_KIND,
  /** Yahtzee score option. */
  YAHTZEE,
  /** Chance score option. */
  CHANCE
}
